{
  "name": "milk-subscription-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Milk Subscription Platform - Complete solution for milk delivery management",
  "scripts": {
    "build": "pnpm -r build",
    "dev:api": "pnpm --filter api dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "eslint": "^8.55.0",
    "prettier": "^3.1.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
